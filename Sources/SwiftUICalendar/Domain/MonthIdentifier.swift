import SwiftCommons

/// Calendar month identity, including the era and leap-month distinction.
/// Use `CalendarViewModel.visibleMonth` when referring to an existing month.
public typealias MonthIdentifier = SwiftCommons.MonthIdentifier
